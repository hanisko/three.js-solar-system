# Three.js/WebGL solar system

## Installation

Install dependencies:

```
npm install
```

Compile the code for development and start a local server at http://localhost:8080/:

```
npm start
```

Create the build:

```
npm run build
```